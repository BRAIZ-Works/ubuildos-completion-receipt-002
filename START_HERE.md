# Follow-Up Prioritizer™ — Start Here

1. Open `app/index.html` in a modern browser.
2. Review the ranked synthetic queue and the visible reason chips.
3. Edit only `data/sample-data.json` or use the UI's demo records for inspection.
4. No network, account, API key, installation, or external service is required.
